// import * as wkt from '@bufbuild/protobuf/wkt'; - There is a chance we need types to be discovered from the registry?
import { createMutableRegistry } from '@bufbuild/protobuf';

export const registry = createMutableRegistry();
